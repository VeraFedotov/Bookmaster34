<Page x:Class="Bookmaster34.View.Pages.ManageCustomersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Bookmaster34.View.Pages"
      mc:Ignorable="d" 
               d:DesignHeight="1004" d:DesignWidth="1920"
    Title="Клиенты">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>


        <Grid Margin="20 0 20 0" Name="ManageCustomersSearch">
            <Grid.RowDefinitions>
                <RowDefinition Height="80"/>
                <RowDefinition Height="60"/>
                <RowDefinition Height="60"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="60"/>
                <RowDefinition Height="60"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="70"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="70"/>
                <RowDefinition Height="20"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="1" Text="Клиент ID:" />
            <TextBox Grid.Row="2" Name="CustomerIDTb"/>

            <TextBlock Grid.Row="4" Text="Имя клиента:" />
            <TextBox Grid.Row="5" Name="NameCustomerTb"/>


            <Button Name="SearchCustomersBtn" Grid.Row="7" 
        Content="Поиск" Click="SearchCustomersBtn_Click"/>

            <Grid Grid.Row="9">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="150"/>
                </Grid.ColumnDefinitions>


                <Button Name="EditCustomersBtn" Grid.Column="0" 
                  Content="Редактировать" Click="EditCustomersBtn_Click"/>

                <Button Name="AddCustomersBtn" Grid.Column="2" 
                 Content="Добавить" Click="AddCustomersBtn_Click"/>

            </Grid>
            

        </Grid>
        <Grid Grid.Column="2">
            <ListView Name="CustomerLv" SelectionChanged="CustomerLv_SelectionChanged" >
                <!-- Создаем заголовки для таблицы -->
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}"/>
                        <GridViewColumn Header="Имя" DisplayMemberBinding="{Binding Name}"/>
                        <GridViewColumn Header="Адрес" DisplayMemberBinding="{Binding Address}"/>
                        <GridViewColumn Header="Почтовый индекс" DisplayMemberBinding="{Binding Zip}"/>
                        <GridViewColumn Header="Город" DisplayMemberBinding="{Binding City.Name}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>

    </Grid>
    </Page>
